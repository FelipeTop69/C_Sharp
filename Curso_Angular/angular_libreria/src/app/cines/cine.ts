export interface CineDTO {
    id: number;
    nombre: string;
    latidud: number;
    longitud: number;
}

export interface CineCreacionDTO {
    nombre: string;
    latidud: number;
    longitud: number;
}